spring:
  cloud:
    gateway:
      globalcors: # 全局的跨域处理
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns: "*" # 允许哪些网站的跨域请求
            allowedMethods: # 允许的跨域ajax的请求方式
              - "GET"
              - "POST"
              - "DELETE"
              - "PUT"
              - "OPTIONS"
            allowedHeaders: "*" # 允许在请求中携带的头信息
            allowCredentials: true # 是否允许携带cookie
            maxAge: 3600000 # 这次跨域检测的有效期
        add-to-simple-url-handler-mapping: true
      routes: # 网关路由配置
        - id: tktk-service-user
          uri: lb://tktk-service-user
          predicates:
            - Path=/tktk/user/**
        - id: tktk-service-video
          uri: lb://tktk-service-video
          predicates:
            - Path=/tktk/video/**
        - id: tktk-service-interact
          uri: lb://tktk-service-interact
          predicates:
            - Path=/tktk/interact/**
        - id: tktk-service-search
          uri: lb://tktk-service-search
          predicates:
            - Path=/tktk/search/**
logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info
# Bug: Wrong timeout config
ribbon:
  ReadTimeout: 1